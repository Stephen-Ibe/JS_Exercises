// Create a function that takes a number as an argument, increments the number by +1 and returns the result.

// (addition(2), 3, "2 plus 1 equals 3.")
// (addition(-9), -8, "-8 plus 1 equals 9.")
// (addition(0), 1, "0 plus 1 equals 1.")

const addition = (num) => {return ++num}
